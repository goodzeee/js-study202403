
var result;   //변수 선언 (저장할 공간 선언)
result = 10 + 20;  //값을 변수에 할당 (우항 값을 좌항에 있는 공간에 저장)

var multiple = result * 3;
console.log(multiple);

var fruit;  // 변수 선언 (데이터 저장 공간에 적합한 이름 짓기)
fruit = '레드향'; //변수 할당
console.log(fruit); 

var food = '볶음면';  //변수 선언 & 할당
food = '멍게';   //변수 재할당
console.log(food); //멍게 출력

// @@ 변수 이름 규칙 @@
// var 7number; // 숫자 맨 앞에 사용 ( x )
var num7ber; // ( o )
var login_user_phone_number; //언더바 사용 = snake case
var loginUserPhoneNumber;   // JS -> camel case 사용 !!
var $login_; // 특문 $, _만 사용 가능
var apple; var APPLE;  // 대소문자 구분하여 다른 변수임.
// var for; // 예약어 키워드 사용 불가능.

var myMoney = 1000;

const $orange = '#ffa808'
console.log($orange);

