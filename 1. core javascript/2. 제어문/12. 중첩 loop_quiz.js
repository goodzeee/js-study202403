var line1 = 3//+prompt('한 변(1)')
var line2 = 4//+prompt('한 변(2)')
var result = '';
// 3 x 4

for(var x = 0; x <= line1; x++ ) { //엔터
  console.log(`\n`);
  for (var y = 0; y <= line2; y++) { //별만
    console.log(`*`);
  }
}